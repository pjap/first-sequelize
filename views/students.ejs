<!DOCTYPE html>
<html>
  <head>
    <meta charset = "utf-8">
    <title><%=title %></title>
    <style>
      table {
        font-family: arial, sans-serif;
        border-collapse: collapse;
        width: 100%;
        font-size: 14px;
      }
      h1 {
        text-transform: uppercase;
        text-align: center;
        font-size: 25px
      }
      a {
        text-decoration: none;
        color: white;
      }
      td, th {
        border: 1px solid #dddddd;
        text-align: center;
        padding: 10px;
      }
    tr:nth-child(even) {
        background-color: #dddddd;
      }
      .button {
        background-color: tomato; /* Green */
        border: none;
        color: white;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 10px;
        margin: 4px 2px;
        cursor: pointer;
      }
      .button2 {
        background-color: tomato; /* Green */
        border: none;
        color: white;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 15px;
        margin: 4px 2px;
        cursor: pointer;
      }
    </style>
  </head>

  <body>
    <h1>Halaman Students</h1>
    <a href = "/">Back To Home</a>
    <table>
      <thead>
        <tr>
          <th>ID</th>
          <!-- <th>FIRST NAME</th>
          <th>LAST NAME</th> -->
          <th>FULL NAME</th>
          <th>EMAIL</th>
          <th>STATUS</th>
          <th>CREATED AT</th>
          <th>UPDATED AT</th>
          <th>STATUS</th>
        </tr>
      </thead>
      <tbody>
        <%data.forEach( row => { %>
        <tr>
          <td><%= row.id %></td>
          <!-- <td><%= row.first_name %></td>
          <td><%= row.last_name %></td> -->
          <td><%= row.first_name%> <%= row.last_name %></td>
          <td><%= row.email %></td>
          <td><%= row.createdAt %></td>
          <td><%= row.updatedAt %></td>
          <td>
            <a href="/students/edit/<%=row.id%>"><button class="button">Edit</button></a>
            <a href="/students/delete/<%=row.id%>"><button class="button">Hapus</button></a>
          </td>
        </tr>
        <% }) %>
      </tbody>
    </table>
    <div style="padding-left:66.4%;padding-top:10px;">
      <a href = "/" style="color:black"><button class="button2">Back To Home</button></a>
      <a href = "/teachers" style="color:black"><button class="button2">Back To Teacher</button></a>
      <a href = "/subjects" style="color:black"><button class="button2">Back To Subjects</button></a>
    </div>
    <form class="" style="margin-top:10px;" action="/students" method="POST">
      <input type="text" name="first_name" placeholder="Masukan Nama Disini">
      <input type="text" name="last_name" placeholder="Masukan Nama Belakang">
      <input type="text" name="email" placeholder="Masukan Email">
      <input type="submit" value="SUBMIT">
    </form>
  </body>
</html>
